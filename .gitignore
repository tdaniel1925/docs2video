
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
# debug
# Deck fit check output — regenerate with scripts/deck-fit-check.mjs
# Demo/test media files
# dependencies
# engine (look, subject, mergedInto, mergedWhy), so these are the notebook and
# env files
# generated job payloads/bundles
# Generated teaser assets
# Head-to-head engine test images — regenerate with scripts/engine-bakeoff.mjs
# large media files (generated, can be recreated)
# Local design-template references (not app code)
# local secrets (platform key + internal secret, never commit)
# misc
# next.js
# not the record.
# one-off verification screenshots from smoke/check scripts
# One-off VPS patch scripts (canonical server lives in vps/server.js)
# Playwright
# production
# quarantined customer correspondence / PII (never commit)
# scratch: copy/brief text dumps, sample PDFs, ad-hoc render + screenshot dumps
# Screenshots taken while checking layouts
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# temp files
# testing
# typescript
# vercel
# Video service secrets
# Working data from the style split — the decisions themselves live in the
*.pem
*.tmp
*.tsbuildinfo
*-brief.pdf
*-copy.txt
*-text.txt
.bakeoff/
.build-context/
.deckcheck/
.DS_Store
.env*
.env*.local
.env.local
.env.production
.logo-fix/
.logo-lab/
.pnp.*
.pnpm-debug.log*
.private/
.shots/
.styles/
.vercel
.yarn/*
/.next/
/.pnp
/build
/coverage
/node_modules
/out/
_review/
check-*.png
comprehension.json
demo-*.mp4
demo-annuity.pdf
logo-lab/results.json
logo-lab/runs/
motion-sample.mp4
music/
next-env.d.ts
npm-debug.log*
out-*.png
pics/
playwright-report/
public/industry-demos/
public/music/
public/test-*.mp4
public/test-*.png
refs/
smoke-*.png
teaser-output/
test-overlay-*.png
test-results/
video-service/docker-compose.yml
video-service/start.sh
vps-*.js
vps-*.py
vps-*.sh
vps/.job/
vps-start-container.sh
yarn-debug.log*
yarn-error.log*
